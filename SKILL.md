---
name: tracing-research-lineages
description: Use when a user supplies, uploads, links, or identifies an academic paper and asks to identify its transferable core mechanism, earliest evidence, causal validation, research lineage, or downstream applications across models, modalities, tasks, attacks, or defenses.
---

# 学术论文研究谱系追踪

## 核心原则

把论文作为承载研究主张的证据单元来追踪，而不是孤立的引用节点。研究谱系的主线由可迁移的核心发现和机制界定；目标论文的模型、模态、任务、威胁场景和实现算法必须与核心方向分层记录。优先使用全文而不是仅摘要证据；论文原始标题保留原文，不要用中文翻译替换原题；最终报告使用用户指定的语言。用户使用中文时，章节标题、字段名、表头和比较说明统一使用中文，并在需要时为英文论文标题补充“中文标题释义”。开始检索或筛选前先阅读[研究协议](references/research-protocol.md)，起草报告前先阅读[输出模板](references/output-template.md)。

## 输出语言与标题规则

- 用户使用中文且未指定其他语言时，最终报告默认使用中文。
- 九个章节名称、所有字段名、表头、逐行比较说明和关系标签，必须采用[输出模板](references/output-template.md)中的中文名称，不得只输出英文标签。
- 论文标题使用“中文标题释义（原始标题）”格式；原始标题必须保留，中文释义不能替代原题或作为检索依据。
- 论文原题、作者名、期刊/会议名、方法名、数据集名、指标名和检索关键词可保留其正式英文写法，但首次出现时应补充中文说明。

## 核心方向抽取：跨应用、分层抽象

核心方向必须概括目标论文最具可迁移性的研究发现或机制，不能被目标模型、模态、任务、威胁场景、数据集、指标或实现算法限定。仍然使用 `研究对象 + 核心机制 + 目的`，但目的只用于说明研究用途，不得替代机制。

先将论文拆成四层，再决定哪些内容进入核心方向：

1. **研究发现层**：论文发现了什么可跨模型、跨模态或跨任务检验的现象、结构或规律？
2. **机制层**：什么内部操作、信息流、优化过程或因果关系解释该发现？
3. **实现层**：论文使用了什么算法、激活、损失、模块或阈值实现/验证该机制？
4. **应用层**：该机制被用于什么模型、模态、任务、攻击、防御、对齐或评测？

“核心研究方向”主要由前两层构成；第三层写入“目标论文实例化”；第四层写入“应用分支”。应用分支不能替代核心方向，但使用同一核心机制的检测、攻击、对齐、编辑和评估论文仍应纳入谱系的应用部分。

按以下顺序抽取并记录：

1. **先写可迁移机制陈述**：用一句话描述“什么内部对象呈现什么可操作的结构或信息流，并产生什么可验证效果”。对于发现型论文，机制可以是“安全相关行为集中于少量内部单元”这类可检验结构性机制，但必须同时说明如何定位和验证。
2. **执行抽象净化测试**：暂时删除模型名称、模态、任务、攻击/防御场景、数据集、指标和具体算法。如果表述仍然准确、可证伪并能解释贡献，则保留为核心方向；如果只剩宽泛主题，则回到机制证据，不要把被删除的应用细节重新塞回核心方向。
3. **分离目标论文实例化**：记录目标论文如何把核心机制落到具体模型、数据、算法和任务上。实现组件不能因为是论文的主要实验方法就自动升级为核心方向。
4. **核对机制证据**：至少对照核心主张、方法/算法/架构描述和实验验证；优先使用消融、替换、剪枝、activation patching、探针、迁移或必要性实验。标题、任务名称、数据集、模型名称和最终目标本身不能作为机制证据。
5. **选择主机制**：若论文同时包含发现、验证和应用，优先选择能解释论文核心发现且可迁移的机制；训练目标、工程组件和应用检测器列为次要机制或应用分支。无法区分时，输出候选抽象及歧义，不要默默退回宽泛主题。
6. **用机制筛选候选**：核心发现和机制论文必须通过同机制或相似机制门槛；应用论文可以改变模型、模态或任务，但必须保留与核心方向对应的内部对象、关键作用或信息/因果流。只共享主题、任务、目标或数据集的论文放入邻近工作。

当前方向的示例：

```text
核心研究方向：大模型内部安全机制的神经元级稀疏性——安全相关行为由少量安全关键神经元或内部功能单元承载，并可通过定位和因果干预得到验证。
目标论文实例化：将该发现用于 VLM 越狱检测；token-wise 激活差分是定位方法，参考向量余弦比较是检测器实现。
应用分支：运行时检测、选择性安全微调、神经元剪枝攻击、神经元恢复、跨模态迁移和神经元引导安全评测。
```

核心机制自检必须回答两个问题：

- 如果移除或替换该研究发现/内部机制，论文的中心发现或解释是否失效或显著改变？
- 如果只替换目标论文的模型、模态、任务或实现算法，核心方向是否仍然成立？

若只有第二个问题的答案为“否”，所写内容通常是应用实现而不是核心方向。若缺少方法和实验/消融证据，必须将机制标为“暂定”或“含混”，并保留候选抽象。

## 分析流程

1. 根据协议判定证据可获得性，并在报告开头说明任何降级模式。
2. 从最完整的可用文本中提取目标论文的研究问题、核心主张及其与实验的对应关系。
3. 按“核心方向抽取：跨应用、分层抽象”先判定研究发现和主机制，再单独记录目标论文实例化与应用分支。
4. 先用核心机制及其同义词建立跨模型、跨模态、跨任务的概念检索式，再用应用词补充检测、攻击、对齐和评估分支；最后通过后向引用和前向引用追踪扩展候选集。综述只用于发现候选论文，核心事实必须回到原始论文记录核验。
5. 区分核心发现/机制论文、机制扩展论文和应用利用论文。核心时间线应覆盖发现、验证、形成、应用和修正，但不能让某一个应用场景定义整条谱系；宽泛或仅主题相似的工作移至邻近工作并说明理由。
6. 按最早可核验的公开日期排列核心论文，同时单独列出正式发表信息。合并同一工作的不同版本，并标出未解决的日期冲突。
7. 对每个核心节点说明继承基础、既有限制、新增量和关系类型。只有存在明确证据时，才声称直接继承。
8. 明确标注分析性推断；证据不完整时说明不确定性；没有充分依据时不要声称检索覆盖完整。

| 关注事项 | 最低要求 |
| --- | --- |
| 最佳证据 | 优先使用全文和已核验的元数据 |
| 验证 | 将每个主要主张与支持它的实验对应起来 |
| 检索 | 机制优先的概念检索加后向/前向引用追踪 |
| 范围 | 任何论文进入核心候选、核心集合、核心时间线或谱系叙述前，先按机制匹配筛选 |
| 时间顺序 | 使用首次公开日期，单独列出正式发表信息 |
| 报告 | 标明关系类型，给出证据引用，暴露不确定性 |

例如，对于一篇声称发现 safety neurons 的论文，应先抽取“安全相关行为集中于少量可定位、可干预的模型内部单元”，再分别追踪早期稀疏安全区域证据、神经元级因果验证、选择性安全对齐、神经元剪枝攻击、运行时检测和神经元引导评测。目标论文使用的模型、模态和检测器应记录为应用实例，而不能替代核心方向。

常见错误包括：把研究主题、任务目标、数据集或模型名称当作核心机制；把目标论文的实现算法或应用场景写成核心方向；把描述性发现与应用防御器混为一谈；只用标题或摘要命名机制；把共享任务或目标的论文当作机制相似论文；跳过机制门槛后把主题相似论文写入核心时间线或谱系叙述；把综述直接当作证据，只使用发表载体日期，把邻近工作硬塞进核心链条，在没有证明时推断直接继承，或在缺少全文、检索来源受限时暗示已经穷尽所有相关工作。

## 质量门槛

- 本次运行已阅读并遵循两份参考文件。
- 起草时逐字采用输出模板中的中文章节名称、字段名和表头，不缩写、不改名；论文原始标题按“中文标题释义（原始标题）”格式呈现。
- 每个有证据支持的主张都有引用，每个推断都明确标为“分析性推断”。
- 目标论文的主张—实验对应关系清晰可见。
- 核心方向已明确写出主机制、机制证据和主机制判定依据；目的没有替代机制。
- 核心研究方向、目标论文实例化和应用分支已经分开记录；模型、模态、任务和实现算法没有反向限定核心方向。
- 对发现型论文已区分最早直接证据、最早明确命名、最早因果验证和最早应用，不把“首个发现”未经拆分地写成绝对结论。
- 对“稀疏”“少量”等比例结论已记录内部单元定义、分母、模型结构、数据条件和安全指标，未直接合并不可比的百分比。
- 核心候选、核心集合、核心时间线和谱系叙述均通过同机制或相似机制匹配门槛；仅共享主题、任务或目标的论文被放入邻近工作。
- 机制证据不足或存在多种解释时，明确报告“暂定”或“含混”状态和候选抽象，不强行给出单一机制。
- 核心时间线使用最早可核验的公开版本，并注明冲突。
- 报告说明检索饱和状态、不确定性、截止日期和覆盖限制，不做无条件的完整性声明。
