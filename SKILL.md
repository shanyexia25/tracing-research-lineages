---
name: tracing-research-lineages
description: Use when a user supplies, uploads, links, or identifies an academic paper and asks about its research question, claims, experimental validation, related-paper timeline, comparative innovations, research lineage, or historical development.
---

# 学术论文研究谱系追踪

## 核心原则

把论文作为承载研究主张的证据单元来追踪，而不是孤立的引用节点。研究谱系的主线由目标论文的核心机制界定，而不是由宽泛主题或任务名称界定。优先使用全文而不是仅摘要证据；论文原始标题保留原文，不要用中文翻译替换原题；最终报告使用用户指定的语言。用户使用中文时，章节标题、字段名、表头和比较说明统一使用中文，并在需要时为英文论文标题补充“中文标题释义”。开始检索或筛选前先阅读[研究协议](references/research-protocol.md)，起草报告前先阅读[输出模板](references/output-template.md)。

## 输出语言与标题规则

- 用户使用中文且未指定其他语言时，最终报告默认使用中文。
- 九个章节名称、所有字段名、表头、逐行比较说明和关系标签，必须采用[输出模板](references/output-template.md)中的中文名称，不得只输出英文标签。
- 论文标题使用“中文标题释义（原始标题）”格式；原始标题必须保留，中文释义不能替代原题或作为检索依据。
- 论文原题、作者名、期刊/会议名、方法名、数据集名、指标名和检索关键词可保留其正式英文写法，但首次出现时应补充中文说明。

## 核心方向抽取：机制优先

核心方向不是论文主题、任务目标、数据集、模型名称或应用场景的概括。必须以目标论文产生主要效果的**核心机制**为中心，表示为：`研究对象 + 核心机制 + 目的`。即使论文主要解释、测量或刻画某个现象，也不能用“现象”填充核心机制字段；如果没有可识别的机制，应标为机制证据不足或含混，把现象记录在研究问题、范围或邻近方向中，并降低后续检索与筛选强度。

按以下顺序抽取并记录：

1. **先写机制陈述**：用一句话描述“什么对象经过什么可操作的变换、交互、信息流、优化步骤或因果操作，产生什么中间状态或效果”。机制名称应能回答“这篇论文凭什么有效”，不能只回答“它研究什么”。
2. **核对机制证据**：至少对照核心主张、方法/算法/架构描述和实验验证；优先使用消融、替换、对照或必要性实验判断某一步是否真正承担主要作用。标题、任务名称、数据集、模型名称和最终目标本身不能作为机制证据。全文不足时，按证据等级降级，并把机制标为暂定或含混。
3. **选择主机制**：若论文包含多个步骤，选择直接支撑中心贡献且有证据支持的主机制；把训练目标、工程组件或次要模块列为次要机制，不让它们替代主机制。若无法区分，输出候选机制及歧义，不要默默退回宽泛主题。
4. **再拼接方向**：将主机制放入 `研究对象 + 核心机制 + 目的`，其中目的只用于限定应用边界，不能替代核心机制。机制同义词优先于任务或领域同义词。
5. **用机制筛选候选**：任何论文进入核心候选、核心集合、核心时间线或发展谱系叙述前，都必须通过机制匹配门槛。只有使用同一机制，或虽实现不同但承担相同功能并保留关键作用/信息流的论文，才可进入核心集合；只共享研究对象、任务、目标或数据集而没有机制对应关系的论文放入邻近工作。

核心机制自检：如果移除或替换所写机制，论文的中心主张是否应当失效或显著改变？原论文是否有方法描述和实验/消融证据支持这个判断？若第二个问题无法回答，必须降低机制判定强度并标为“暂定”或“含混”，而不是用宽泛主题填空。

## 分析流程

1. 根据协议判定证据可获得性，并在报告开头说明任何降级模式。
2. 从最完整的可用文本中提取目标论文的研究问题、核心主张及其与实验的对应关系。
3. 按“核心方向抽取：机制优先”先判定主机制及其证据状态，再建立“研究对象 + 核心机制 + 目的”的核心方向，并明确范围、同义词和排除项。
4. 以核心机制及其同义词为主建立概念检索式，再用研究对象和目的限制范围，最后通过后向引用和前向引用追踪扩展候选集。综述只用于发现候选论文，核心事实必须回到原始论文记录核验。
5. 区分核心论文与邻近工作。核心时间线只保留与谱系直接相关的论文，宽泛或仅主题相似的工作移至邻近工作并说明理由。
6. 按最早可核验的公开日期排列核心论文，同时单独列出正式发表信息。合并同一工作的不同版本，并标出未解决的日期冲突。
7. 对每个核心节点说明继承基础、既有限制、新增量和关系类型。只有存在明确证据时，才声称直接继承。
8. 明确标注分析性推断；证据不完整时说明不确定性；没有充分依据时不要声称检索覆盖完整。

| 关注事项 | 最低要求 |
| --- | --- |
| 最佳证据 | 优先使用全文和已核验的元数据 |
| 验证 | 将每个主要主张与支持它的实验对应起来 |
| 检索 | 机制优先的概念检索加后向/前向引用追踪 |
| 范围 | 任何论文进入核心候选、核心集合、核心时间线或谱系叙述前，先按机制匹配筛选 |
| 时间顺序 | 使用首次公开日期，单独列出正式发表信息 |
| 报告 | 标明关系类型，给出证据引用，暴露不确定性 |

例如，对于一篇多模态越狱论文，应识别其确切攻击机制，将每个主张与实验对应起来，通过引用追踪寻找前驱攻击形式和后续防御工作，再把基准测试或联系较弱的安全工作放入邻近工作，而不是核心谱系。

常见错误包括：把研究主题、任务目标、数据集或模型名称当作核心机制；把现象直接填入核心机制字段；只用标题或摘要命名机制；把共享任务或目标的论文当作机制相似论文；跳过机制门槛后把主题相似论文写入核心时间线或谱系叙述；把综述直接当作证据，只使用发表载体日期，把邻近工作硬塞进核心链条，在没有证明时推断直接继承，或在缺少全文、检索来源受限时暗示已经穷尽所有相关工作。

## 质量门槛

- 本次运行已阅读并遵循两份参考文件。
- 起草时逐字采用输出模板中的中文章节名称、字段名和表头，不缩写、不改名；论文原始标题按“中文标题释义（原始标题）”格式呈现。
- 每个有证据支持的主张都有引用，每个推断都明确标为“分析性推断”。
- 目标论文的主张—实验对应关系清晰可见。
- 核心方向已明确写出主机制、机制证据和主机制判定依据；目的没有替代机制。
- 核心候选、核心集合、核心时间线和谱系叙述均通过同机制或相似机制匹配门槛；仅共享主题、任务或目标的论文被放入邻近工作。
- 机制证据不足或存在多种解释时，明确报告“暂定”或“含混”状态和候选抽象，不强行给出单一机制。
- 核心时间线使用最早可核验的公开版本，并注明冲突。
- 报告说明检索饱和状态、不确定性、截止日期和覆盖限制，不做无条件的完整性声明。
